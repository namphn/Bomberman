package Interface;import Object.Scores;import Interface.GameScreen;import com.sun.xml.internal.ws.api.model.MEP;import javax.swing.*;import java.awt.*;import java.awt.event.MouseAdapter;import java.awt.event.MouseEvent;public class HighScoresPane {    private Scores scores;    private JPanel panel;    private JLabel background;    private JLabel backButton;    private JLabel menu;    private Thread thread;    public HighScoresPane(Scores sc, Menu menu1){        scores = sc;        panel = pnHighScore;        menu = new JLabel();        panel.setLayout(null);        //background = new JLabel();        backButton = new JLabel();        backButton = setImageIcon(827,564,"/Images/backbutton.png");        panel.add(backButton);        setClick(new ClickEffect(menu1));    }    public JLabel setImageIcon(int x, int y, String source){        JLabel jLabel = new JLabel();        ImageIcon imageIcon = new ImageIcon(getClass().getResource(source));        jLabel.setBounds(x,y,imageIcon.getIconWidth(),imageIcon.getIconHeight());        jLabel.setIcon(imageIcon);        return jLabel;    }    public void  drawImage(Graphics2D g2d){        Image background = new ImageIcon(getClass().getResource("/Images/menu_background.png")).getImage();        Image backgrLb = new ImageIcon(getClass().getResource("/Images/highscore.png")).getImage();        g2d.drawImage(background, 0, 0, null);        g2d.drawImage(backgrLb, 55, 40, null);    }    private JPanel pnHighScore = new JPanel() {        @Override        protected void paintComponent(Graphics g) {            super.paintComponent(g);            Graphics2D g2d = (Graphics2D) g;            drawImage(g2d);            if (scores != null) {                drawHightSore(g2d);            }        }    };    public void drawHightSore(Graphics2D g2d) {        g2d.setStroke(new java.awt.BasicStroke(2));        g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);        g2d.setFont(new Font("Arial", Font.BOLD, 30));        g2d.setColor(Color.YELLOW);        int y = 170;        for (int i = 0; i < 5; i++) {            g2d.drawString("" + (i + 1), 195, y);            g2d.drawString("" + scores.getAllName().get(i), 330, y);            g2d.drawString("" + scores.getAllScores().get(i), 665, y);            y = y + 45;        }    }    public JPanel getPanel() {        return panel;    }    public void setClick(MouseAdapter click){        backButton.addMouseListener(click);    }    public JLabel getBackButton() {        return backButton;    }}