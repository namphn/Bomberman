package Interface;import Effect.CacheDataLoader;import Interface.GamePanel;import Map.Manager;import javafx.scene.layout.Background;import javax.swing.*;import java.awt.*;import java.io.IOException;public class GameScreen extends JFrame {    /**     * chiều rộng : SCREEN_WIDTH     * chiều cao : SCREEN_HEIGHT     * của cửa sổ game     */    public static final int SCREEN_WIDTH = 950;    public static final int SCREEN_HEIGHT = 680;    private Menu menu;    //private Manager mMagager = new Manager(1);    /**     * khoi tao màn hình game     */    //private GamePanel gamePanel;    /**     * phương thức khởi tạo một JFRAME (1 cửa sổ game)     * vì Interface.GameScreen kế thừa từ Jrame nên bản thân nó cũng là một Jrame rồi     */    public GameScreen(){        setTitle("Bomber Man");      // set title cho cửa sổ        setSize(SCREEN_WIDTH,SCREEN_HEIGHT);     // set chiều rộng và chiều cao        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);      // set nút close        setLocationRelativeTo(null);                         // cho cửa sổ suất hiện chính giữa        setResizable(false);                                 // không cho thay đổi kích cỡ cửa sổ        try {            CacheDataLoader.getInstance().LoadData();        } catch (IOException e) {            e.printStackTrace();        }        //menu = new Menu(this);        //gamePanel = new GamePanel();        //add(gamePanel);                                         // thêm panel vào frame        //addKeyListener(menu.getGamePanel());                              // thêm một bộ lắng nghe sự kiện        //setVisible(true);        //startGame();    }    /**     * phuong thức start của panel     */    public Menu getMenu() {        return menu;    }}