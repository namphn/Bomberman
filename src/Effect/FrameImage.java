package Effect;import java.awt.*;import java.awt.image.BufferedImage;/** * class nay dùng để tạo ra frame mỗi frame là 1 hình */public class FrameImage {    private String name;    private BufferedImage image;    public FrameImage(String name, BufferedImage image){        this.name = name;        this.image = image;    }    /**     * phuowng thức copy     * @param frameImage     */    public FrameImage(FrameImage frameImage){        /**         *không nên dùng như này, vì khi gán như này thì tức là để 2 tham chiếu cùng         * tham chiếu vào một đối tượng, nên ta sẽ vẽ cái image mới lên image cũ         */        /*this.image = frameImage.getImage();        this.name = frameImage.getName();*/        /**         * tạo ra một BufferedImage với các thông số chiều rộng, chiều cao và kiểu         */        if(frameImage.image==null) System.out.println("null image");        image = new BufferedImage(frameImage.getImageWidth(), frameImage.getImageHeight(),                                        frameImage.getImage().getType());        /**         * khi get một graphic từ bất kì một đối tượng nào có thể vẽ được thì graphic này như một         * cái chổi để vẽ lên cái image đó         */        Graphics g = image.getGraphics();        g.drawImage(frameImage.getImage(),0,0, null);    }    /**     * hàm draw image ở trung tâm hình ảnh     * @param g2     * @param x     * @param y     */    public void draw(Graphics2D g2, int x, int y){        g2.drawImage(image, (x - image.getWidth())/2,( y - image.getHeight())/2, null);    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public void setImage(BufferedImage image) {        this.image = image;    }    public BufferedImage getImage() {        return image;    }    public int getImageHeight() {        if(image == null) System.out.println("null");        return image.getHeight();    }    public int getImageWidth() {        //if(image == null) System.out.println("null");        return image.getWidth();    }    public FrameImage(){        image = null;        name = null;    }}